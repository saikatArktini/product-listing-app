# Product Listing Application

A full-stack product listing application built with **Next.js (App Router), React, Prisma, and MongoDB**.

The application supports:

* Product listing
* Category and Subcategory filtering
* Search functionality
* Pagination
* Responsive UI
* Reusable React components
* Custom hooks for API data fetching

---

#  Tech Stack

* Next.js (App Router)
* React
* Prisma ORM
* MongoDB
* Tailwind CSS

---

#  Installation

Clone the repository:

```
git clone <your-repo-url>
```

Install dependencies:

```
npm install
```

Run the development server:

```
npm run dev
```

Open in browser:

```
http://localhost:3000
```

---

#  Environment Variables

Create a `.env` file in the **root directory** of the project.

Required environment variables:

```
DATABASE_URL="your-mongodb-connection-string"
```

Example:

```
DATABASE_URL="DATABASE_URL="mongodb+srv://username:password@productmanagement.fnqjcc7.mongodb.net/mydb?retryWrites=true&w=majority""
```

# Features

* Global product search
* Category filtering
* Subcategory filtering
* Pagination
* Modular component architecture
* Custom reusable hooks
* API service layer
* Clean folder structure

---

# Author

Saikat Ganguly
